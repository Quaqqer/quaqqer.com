import Hero from "@/components/Hero";
import MdxLayout from "@/components/MdxLayout";

import { ProjectsGrid } from "./projects";

export default function({children}) {
    return (
      <div className="pb-24 mx-auto max-w-7xl px-6 lg:px-8">
        <Hero size="md" title="Projects" description="Here are some of the projects I've worked on in my free time. Some of them are just links to GitHub, but some have a demo in the browser, if you see a play button, why don't you click it?" />

        <MdxLayout children={children} />

        <ProjectsGrid />
      </div>
    );

}
